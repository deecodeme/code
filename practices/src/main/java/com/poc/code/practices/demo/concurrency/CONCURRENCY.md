**Runnable** - A task which does not return any value.

**Callable** - A task which may return a value.

**Future** - A future task which can be attempted to complete or cancel.

**Executor** - Base interface which executes a **Runnable**. Intent is to decouple execution from the task.

**ExecutorService** - Extends ˘ with capability of returning **Futures**, invoking computation and shutting down the ExecutorService.
